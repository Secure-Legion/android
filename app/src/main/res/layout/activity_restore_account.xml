<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:fillViewport="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="50dp"
        android:paddingBottom="32dp">

        <!-- Back Button -->
        <View
            android:id="@+id/backButton"
            android:layout_width="35dp"
            android:layout_height="35dp"
            android:layout_marginStart="20dp"
            android:background="@drawable/back_button_bg"
            android:clickable="true"
            android:focusable="true"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="@id/titleText" />

        <!-- Title -->
        <TextView
            android:id="@+id/titleText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Import Account"
            android:fontFamily="@font/space_grotesk_bold"
            android:textSize="20sp"
            android:textColor="?attr/colorOnSurfaceVariant"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Account Recovery Seed Heading -->
        <TextView
            android:id="@+id/seedHeading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="32dp"
            android:layout_marginTop="48dp"
            android:text="Account Recovery Seed"
            android:fontFamily="@font/space_grotesk_bold"
            android:textSize="16sp"
            android:textColor="?attr/colorOnSurfaceVariant"
            app:layout_constraintTop_toBottomOf="@id/titleText"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Seed Phrase Input (single box — paste all 12 words) -->
        <EditText
            android:id="@+id/seedPhraseInput"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:minHeight="140dp"
            android:layout_marginStart="32dp"
            android:layout_marginEnd="32dp"
            android:layout_marginTop="16dp"
            android:padding="16dp"
            android:hint="Enter or paste your 12-word seed phrase"
            android:textColorHint="@color/lock_password_hint"
            android:textColor="?attr/colorOnSurface"
            android:fontFamily="@font/poppins_medium"
            android:textSize="16sp"
            android:lineSpacingExtra="6dp"
            android:background="@drawable/lock_password_input_bg"
            android:inputType="textMultiLine"
            android:gravity="top|start"
            app:layout_constraintTop_toBottomOf="@id/seedHeading"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Seed Phrase Description -->
        <TextView
            android:id="@+id/seedDescriptionText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="32dp"
            android:layout_marginEnd="32dp"
            android:layout_marginTop="16dp"
            android:text="Enter your 12-word seed phrase to import your account. This will import your messaging identity and attempt to recover your contacts."
            android:fontFamily="@font/poppins_regular"
            android:textSize="13sp"
            android:textColor="@color/lock_subtitle_gray"
            app:layout_constraintTop_toBottomOf="@id/seedPhraseInput"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Import Button -->
        <TextView
            android:id="@+id/importButton"
            android:layout_width="0dp"
            android:layout_height="60dp"
            android:layout_marginStart="32dp"
            android:layout_marginEnd="32dp"
            android:layout_marginTop="24dp"
            android:text="Import"
            android:textSize="16sp"
            android:textColor="?attr/colorOnSurface"
            android:fontFamily="@font/space_grotesk_bold"
            android:background="@drawable/lock_button_bg"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true"
            app:layout_constraintTop_toBottomOf="@id/seedDescriptionText"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</ScrollView>
